# 参与贡献

感谢你改进 Rime 中日直输。为了让问题能够复现并避免破坏中文或日语输入，请尽量遵循以下约定。

## 提交问题

请提供：

- 输入码；
- 期望候选及期望顺序；
- 实际候选；
- 是否启用了中文/日语模糊匹配；
- 软件版本和 Windows 版本。

请勿上传包含个人输入内容、桌面、账号、文件名、日志隐私或本机绝对路径的截图。可以裁剪为只包含候选窗的画面，或使用虚构示例重现问题。

## 提交代码

- 将中文词条、英文词条、Lua、schema 和前端代码分别放入仓库约定目录；
- 不提交 `build/`、用户数据库、同步目录、日志、诊断包或个人常用语；
- 提交信息使用 Conventional Commits；
- 运行与修改范围对应的格式、候选和 Windows TSF 测试；
- 用户可见行为发生变化时，同时更新 README、CHANGELOG 或相关文档。

## 隐私检查

提交前请运行 `git status` 并逐项检查暂存内容。所有公开图片必须只展示产品本身，不得出现桌面、任务栏、账号、个人窗口或机器信息。
